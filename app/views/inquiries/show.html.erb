<p style="color: green"><%= notice %></p>

<div class="d-flex justify-content-between align-items-center">
  <h1>Inquiry Details</h1>
  <div>
    <%= link_to "Back to inquiries", inquiries_path, class: "btn btn-primary" %> |
    <%= button_to "Destroy this inquiry", @inquiry, method: :delete, class: "btn btn-danger" %>
  </div>
</div>

<%= render @inquiry %>